bazel-android-testdpc
bazel-bin
bazel-out
bazel-testlogs
**/*.iml
.idea
# Personal, per-developer preferences
CLAUDE.local.md

# Signing material - never commit. The real keystore lives outside this tree,
# in ~/keystores/, so it cannot be added by accident.
*.jks
*.keystore
*.p12
*.pass
*.idsig
